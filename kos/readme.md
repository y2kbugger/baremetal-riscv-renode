next:
ok so problem is that restarting the "finished" process it goes to the last instruction of the target process, try and fails to return and causes an interrupt.
add timer
implement the "schedule processes function"
    needs a process state flag